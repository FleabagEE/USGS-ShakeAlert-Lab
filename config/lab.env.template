# Non-secret development settings. Copy locally to config/lab.env if needed;
# that destination is ignored by Git.
LAB_BANNER="SHAKEALERT LAB — NO OPERATIONAL OUTPUTS"
ALLOW_OPERATIONAL_OUTPUTS=false
APPLICATION_TIMEZONE=UTC
LAB_MODE=development

